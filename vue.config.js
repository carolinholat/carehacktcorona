/*module.exports = {
    runtimeCompiler: true,
    outputDir: "dist",
    assetsDir: "static",
    publicPath:
        process.env.NODE_ENV === "production"
            ? "/"
            : "/"
}; */

module.exports = {
    runtimeCompiler: true,
    outputDir: "dist",
    assetsDir: "static",
    publicPath:
        process.env.NODE_ENV === "production"
            ? "/"
            : "/"
};